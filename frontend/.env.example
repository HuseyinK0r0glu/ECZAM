# ECZAM frontend environment — copy to `.env` and fill in.
# Only VITE_-prefixed vars are exposed to the browser. Do NOT put secrets here.

VITE_API_URL=http://localhost:8080/api/v1
VITE_VAPID_PUBLIC_KEY=     # mirror of backend VAPID_PUBLIC_KEY (Phase 4)
